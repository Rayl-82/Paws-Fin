const { PrismaClient } = require('@prisma/client');

// Use a simple initialization that works with this version or just use the local ts one.
// Let's create a JS script that imports the lib/prisma.ts using ts-node or next.
